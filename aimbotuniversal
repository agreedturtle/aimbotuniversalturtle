-- =============================================
-- UNIVERSAL ROBLOX AIMBOT + EXTRAS
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

local LocalPlayer = Players.LocalPlayer

-- ==================== SETTINGS ====================
local AimBot = {
    Enabled = false,
    Smoothness = 0.25,
    FOV = 150,
    TargetPart = "Head",
    TeamCheck = false,
    VisibilityCheck = true,
    HoldMode = true,          -- Hold Right Click or Toggle
}

local Combat = {
    RapidFire = false,
    NoReload = false,
}

-- FOV Circle
local fovCircle = Drawing.new("Circle")
fovCircle.Thickness = 2
fovCircle.NumSides = 64
fovCircle.Radius = AimBot.FOV
fovCircle.Filled = false
fovCircle.Transparency = 0.7
fovCircle.Color = Color3.fromRGB(255, 50, 50)
fovCircle.Visible = true

-- ==================== GUI MENU ====================
local lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/UI-Libs/main/Vape.txt"))()
local win = lib:Window("Universal Aimbot", Color3.fromRGB(0, 170, 255), Enum.KeyCode.P)

local aimTab = win:Tab("Aimbot")
local combatTab = win:Tab("Combat")

-- Aimbot Settings
aimTab:Toggle("Enable Aimbot", false, function(s)
    AimBot.Enabled = s
end)

aimTab:Toggle("Hold Mode (Right Click)", true, function(s)
    AimBot.HoldMode = s
end)

aimTab:Slider("Smoothness", 0.05, 1, 0.25, function(v)
    AimBot.Smoothness = v
end)

aimTab:Slider("FOV", 50, 500, 150, function(v)
    AimBot.FOV = v
    fovCircle.Radius = v
end)

aimTab:Dropdown("Target Part", {"Head", "UpperTorso", "HumanoidRootPart"}, function(selected)
    AimBot.TargetPart = selected
end)

aimTab:Toggle("Team Check", false, function(s)
    AimBot.TeamCheck = s
end)

aimTab:Toggle("Visibility Check", true, function(s)
    AimBot.VisibilityCheck = s
end)

aimTab:Toggle("Show FOV Circle", true, function(s)
    fovCircle.Visible = s
end)

-- Combat Features
combatTab:Toggle("Rapid Fire", false, function(s)
    Combat.RapidFire = s
end)

combatTab:Toggle("No Reload / Infinite Ammo", false, function(s)
    Combat.NoReload = s
end)

combatTab:Label("More coming soon...")

print("✅ Universal Aimbot + Menu Loaded! | Press P")

-- ==================== AIMBOT FUNCTIONS ====================
local function isVisible(targetChar)
    if not AimBot.VisibilityCheck then return true end
    local target = targetChar:FindFirstChild(AimBot.TargetPart) or targetChar:FindFirstChild("HumanoidRootPart")
    if not target then return false end

    local origin = Camera.CFrame.Position
    local direction = (target.Position - origin).Unit * 2000

    local raycastParams = RaycastParams.new()
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character}
    raycastParams.FilterType = Enum.RaycastFilterType.Blacklist

    local result = workspace:Raycast(origin, direction, raycastParams)
    return result and result.Instance and result.Instance:IsDescendantOf(targetChar)
end

local function getClosestPlayer()
    local closest, minDist = nil, AimBot.FOV

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hum = player.Character:FindFirstChild("Humanoid")
            local part = player.Character:FindFirstChild(AimBot.TargetPart) or player.Character:FindFirstChild("HumanoidRootPart")

            if hum and hum.Health > 0 and part then
                if AimBot.TeamCheck and player.Team == LocalPlayer.Team then continue end

                local screenPos, onScreen = Camera:WorldToViewportPoint(part.Position)
                if onScreen then
                    local mousePos = UserInputService:GetMouseLocation()
                    local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude

                    if dist < minDist and isVisible(player.Character) then
                        minDist = dist
                        closest = player
                    end
                end
            end
        end
    end
    return closest
end

-- ==================== MAIN AIMBOT LOOP ====================
local aiming = false

RunService.RenderStepped:Connect(function()
    fovCircle.Position = UserInputService:GetMouseLocation()

    if not AimBot.Enabled then return end

    local targetPlayer = getClosestPlayer()

    if targetPlayer and targetPlayer.Character then
        local targetPart = targetPlayer.Character:FindFirstChild(AimBot.TargetPart) or 
                          targetPlayer.Character:FindFirstChild("HumanoidRootPart")

        if targetPart then
            local targetPos = Camera:WorldToViewportPoint(targetPart.Position)
            local mousePos = UserInputService:GetMouseLocation()

            local dx = (targetPos.X - mousePos.X) * AimBot.Smoothness
            local dy = (targetPos.Y - mousePos.Y) * AimBot.Smoothness

            mousemoverel(dx, dy)
        end
    end
end)

-- ==================== KEYBINDS ====================
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end

    if input.UserInputType == Enum.UserInputType.MouseButton2 then  -- Right Click
        if AimBot.HoldMode then
            AimBot.Enabled = true
        end
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 and AimBot.HoldMode then
        AimBot.Enabled = false
    end
end)

-- ==================== RAPID FIRE & NO RELOAD ====================
RunService.Heartbeat:Connect(function()
    if Combat.RapidFire or Combat.NoReload then
        pcall(function()
            local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
            if tool then
                local script = tool:FindFirstChildOfClass("LocalScript") or tool:FindFirstChildOfClass("Script")
                if script then
                    -- Simple rapid fire attempt
                    if Combat.RapidFire then
                        tool:Activate()
                    end
                end
            end
        end)
    end
end)

print("Hold Right Click to Aim | Press P for Menu")
