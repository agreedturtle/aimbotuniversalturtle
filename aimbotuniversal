local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

-- ==================== SETTINGS ====================
local flySpeed = 70
local flying = false

local hitboxEnabled = false
local hitboxSize = 14
local hitboxTransparency = 0.65

local wallhackEnabled = false

-- ==================== FLY ====================
local flyBV, flyBAV

local function startFly()
    if flying then return end
    local char = Player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    
    flying = true
    local hum = char:FindFirstChild("Humanoid")
    if hum then hum.PlatformStand = true end
    
    flyBV = Instance.new("BodyVelocity")
    flyBV.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyBV.Parent = char.HumanoidRootPart
    
    flyBAV = Instance.new("BodyAngularVelocity")
    flyBAV.MaxTorque = Vector3.new(1e5, 1e5, 1e5)
    flyBAV.Parent = char.HumanoidRootPart
end

local function endFly()
    flying = false
    if flyBV then flyBV:Destroy() end
    if flyBAV then flyBAV:Destroy() end
    local hum = Player.Character and Player.Character:FindFirstChild("Humanoid")
    if hum then hum.PlatformStand = false end
end

-- ==================== HITBOX + WALLHACK ====================
local function updateHitboxes()
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= Player and plr.Character then
            local root = plr.Character:FindFirstChild("HumanoidRootPart")
            if root then
                if hitboxEnabled then
                    root.Size = Vector3.new(hitboxSize, hitboxSize * 2.2, hitboxSize)
                    root.Transparency = hitboxTransparency
                    root.CanCollide = false
                    root.Massless = true
                else
                    root.Size = Vector3.new(2, 4, 1)
                    root.Transparency = 0
                    root.CanCollide = true
                end
            end

            -- Wallhack
            if wallhackEnabled then
                for _, part in pairs(plr.Character:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.Transparency = 0.3
                        part.Material = Enum.Material.ForceField
                        part.Color = Color3.fromRGB(0, 255, 150)
                    end
                end
            elseif not hitboxEnabled then
                for _, part in pairs(plr.Character:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.Transparency = 0
                        part.Material = Enum.Material.Plastic
                    end
                end
            end
        end
    end
end

-- ==================== GUI (Kavo UI - More Stable) ====================
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("Strucid ESP", "Ocean")

local MainTab = Window:NewTab("Main")
local FlySection = MainTab:NewSection("Fly")
local HitboxSection = MainTab:NewSection("Hitbox")
local WallhackSection = MainTab:NewSection("Wallhack")

FlySection:NewToggle("Enable Fly", false, function(state)
    if state then
        startFly()
    else
        endFly()
    end
end)

FlySection:NewSlider("Fly Speed", 20, 150, 70, function(value)
    flySpeed = value
end)

HitboxSection:NewToggle("Enable Hitbox", false, function(state)
    hitboxEnabled = state
end)

HitboxSection:NewSlider("Hitbox Size", 6, 25, 14, function(value)
    hitboxSize = value
end)

HitboxSection:NewSlider("Hitbox Transparency", 0, 1, 0.65, function(value)
    hitboxTransparency = value
end)

WallhackSection:NewToggle("Enable Wallhack (Chams)", false, function(state)
    wallhackEnabled = state
end)

-- ==================== LOOPS ====================
RunService.Heartbeat:Connect(function()
    if flying and Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
        local cam = workspace.CurrentCamera
        local moveDir = Vector3.new()

        if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir += cam.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir -= cam.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir -= cam.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir += cam.CFrame.RightVector end

        if moveDir.Magnitude > 0 then
            flyBV.Velocity = moveDir.Unit * flySpeed
        else
            flyBV.Velocity = Vector3.new()
        end
    end
end)

RunService.Stepped:Connect(updateHitboxes)

print("✅ Strucid ESP Fixed & Loaded! | Press Right Ctrl to open menu")
