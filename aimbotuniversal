-- =============================================
-- UNIVERSAL ROBLOX AIMBOT + COMBAT MENU
-- Press P to open menu
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

local LocalPlayer = Players.LocalPlayer

-- ==================== SETTINGS ====================
local Aimbot = {
    Enabled = false,
    Smoothness = 0.22,
    FOV = 180,
    TargetPart = "Head",
    TeamCheck = false,
    VisibilityCheck = true,
    HoldToAim = true,
}

local Combat = {
    RapidFire = false,
    NoReload = false,
    NoRecoil = false,
    Triggerbot = false,
    InfiniteAmmo = false,
}

local Visuals = {
    ShowFOV = true,
    ESP_Enabled = false,
}

-- ==================== KAVO UI LIBRARY ====================
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("Universal Aimbot", "Ocean")

-- ==================== TABS ====================
local AimbotTab = Window:NewTab("Aimbot")
local CombatTab = Window:NewTab("Combat")
local VisualsTab = Window:NewTab("Visuals")
local SettingsTab = Window:NewTab("Settings")

-- ==================== AIMBOT TAB ====================
AimbotTab:NewToggle("Enable Aimbot", false, function(state)
    Aimbot.Enabled = state
end)

AimbotTab:NewToggle("Hold to Aim (Right Click)", true, function(state)
    Aimbot.HoldToAim = state
end)

AimbotTab:NewSlider("Smoothness", 0.05, 1, 0.22, function(value)
    Aimbot.Smoothness = value
end)

AimbotTab:NewSlider("FOV", 50, 600, 180, function(value)
    Aimbot.FOV = value
end)

AimbotTab:NewDropdown("Target Part", {"Head", "UpperTorso", "HumanoidRootPart", "LowerTorso"}, function(currentOption)
    Aimbot.TargetPart = currentOption
end)

AimbotTab:NewToggle("Team Check", false, function(state)
    Aimbot.TeamCheck = state
end)

AimbotTab:NewToggle("Visibility Check", true, function(state)
    Aimbot.VisibilityCheck = state
end)

-- ==================== COMBAT TAB ====================
CombatTab:NewToggle("Rapid Fire", false, function(state)
    Combat.RapidFire = state
end)

CombatTab:NewToggle("No Reload / Infinite Ammo", false, function(state)
    Combat.NoReload = state
end)

CombatTab:NewToggle("No Recoil", false, function(state)
    Combat.NoRecoil = state
end)

CombatTab:NewToggle("Triggerbot", false, function(state)
    Combat.Triggerbot = state
end)

-- ==================== VISUALS TAB ====================
VisualsTab:NewToggle("Show FOV Circle", true, function(state)
    Visuals.ShowFOV = state
end)

VisualsTab:NewToggle("Basic ESP (Boxes)", false, function(state)
    Visuals.ESP_Enabled = state
end)

-- ==================== FOV CIRCLE ====================
local fovCircle = Drawing.new("Circle")
fovCircle.Thickness = 2
fovCircle.NumSides = 80
fovCircle.Radius = Aimbot.FOV
fovCircle.Filled = false
fovCircle.Transparency = 0.75
fovCircle.Color = Color3.fromRGB(255, 0, 100)
fovCircle.Visible = true

-- ==================== MAIN FUNCTIONS ====================
local function isVisible(targetChar)
    if not Aimbot.VisibilityCheck then return true end
    local target = targetChar:FindFirstChild(Aimbot.TargetPart) or targetChar:FindFirstChild("HumanoidRootPart")
    if not target then return false end

    local origin = Camera.CFrame.Position
    local direction = (target.Position - origin).Unit * 3000

    local params = RaycastParams.new()
    params.FilterDescendantsInstances = {LocalPlayer.Character}
    params.FilterType = Enum.RaycastFilterType.Blacklist

    local result = workspace:Raycast(origin, direction, params)
    return result and result.Instance:IsDescendantOf(targetChar)
end

local function getClosestPlayer()
    local closest, minDist = nil, Aimbot.FOV + 1

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hum = player.Character:FindFirstChild("Humanoid")
            local part = player.Character:FindFirstChild(Aimbot.TargetPart) or player.Character:FindFirstChild("HumanoidRootPart")

            if hum and hum.Health > 0 and part then
                if Aimbot.TeamCheck and player.Team == LocalPlayer.Team then continue end

                local screenPos, onScreen = Camera:WorldToViewportPoint(part.Position)
                if onScreen then
                    local mousePos = UserInputService:GetMouseLocation()
                    local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude

                    if dist < minDist and isVisible(player.Character) then
                        minDist = dist
                        closest = player
                    end
                end
            end
        end
    end
    return closest
end

-- ==================== AIMBOT LOOP ====================
RunService.RenderStepped:Connect(function()
    fovCircle.Position = UserInputService:GetMouseLocation()
    fovCircle.Radius = Aimbot.FOV
    fovCircle.Visible = Visuals.ShowFOV

    if not Aimbot.Enabled then return end

    local target = getClosestPlayer()
    if target and target.Character then
        local part = target.Character:FindFirstChild(Aimbot.TargetPart) or target.Character:FindFirstChild("HumanoidRootPart")
        if part then
            local targetPos = Camera:WorldToViewportPoint(part.Position)
            local mousePos = UserInputService:GetMouseLocation()

            local dx = (targetPos.X - mousePos.X) * Aimbot.Smoothness
            local dy = (targetPos.Y - mousePos.Y) * Aimbot.Smoothness

            mousemoverel(dx, dy)
        end
    end
end)

-- ==================== KEYBINDS ====================
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.UserInputType == Enum.UserInputType.MouseButton2 and Aimbot.HoldToAim then
        Aimbot.Enabled = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 and Aimbot.HoldToAim then
        Aimbot.Enabled = false
    end
end)

-- ==================== COMBAT FEATURES ====================
RunService.Heartbeat:Connect(function()
    if not LocalPlayer.Character then return end
    local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if not tool then return end

    if Combat.RapidFire then
        tool:Activate()
    end
end)

print("✅ Universal Aimbot Loaded Successfully!")
print("Press P to open the menu")
